```markdown
# Maps

## What i learnt
Maps are used to store data values, it can be an unordered and changeable it doesn't allow duplication. It length is determine by the number of it elements, it can be found using the len() function.
The defualt value of map is nil


## Example
```go
ages := map[string]int{"Joy": 20, "Ali": 22}
fmt.Println(ages["Joy"])