```markdown

# Struct

## What i learnt
struct are used to create complex data types for storing multiple related items, such as coordinates or user profiles.

## Example
```go

struct Person {
    char name[20];
    int age;
};
struct Person p1 = {"Alice", 30};
