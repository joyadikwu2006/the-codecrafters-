```markdown

# Slices

## What I learnt
Slices are like arrays or a dynamic arrays that can grow or shrink and are flexible.
the two function that for returning the length and capacity of a slice are: len() and cap()

## Example
```go
numbers := []int{1,2,3}
numbers = append(numbers, 4) append is used to add items.