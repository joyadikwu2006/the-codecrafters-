```markdown
# Arrays

## What I Learned
Arrays store multiple values of the same type with fixed size. it can also be store with a difinite length using indexing.

## Example
```go
var numbers [3]int = [3]int{1,2,3}
var name = string{"Joy", "Diamond", "Ochanya"}